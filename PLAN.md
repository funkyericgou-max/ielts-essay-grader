# IELTS AI Grader — 项目纲要

## 项目目标

基于现有的 `index.html` 单文件应用，将其从**写作批改工具**扩展为**写作 + 口语 + 听力三模式 AI 评分与练习工具**。

**核心挑战**：
- 写作：让 AI 评分尽可能接近雅思官方标准（±0.5 分偏差）
- 口语：在零后端、纯前端的限制下，实现录音→转写→评分的完整链路，发音评分依赖间接推断
- 听力：纯客户端游戏化同义替换练习，零 API 调用，保持趣味性和学习价值

## 当前版本 (v1) — 写作评分

### 核心优化组合拳

1. **多任务支持**：UI 提供 Toggle 切换，完美支持 Task 1（小作文）与 Task 2（大作文）两套评分标准
2. **Chain-of-Thought (CoT) 强制推理**：JSON 输出顺序设计为"先分析推理 → 再输出证据 → 最后给出分数"，彻底消除"盲猜"幻觉
3. **全文标签包裹法**：AI 直接返回带 HTML 安全标签的完整作文，前端 `innerHTML` 渲染，零高亮错位
4. **轻量行动导向**：砍掉冗余的 4 周训练计划，改为 3 条精准的"下一步核心行动建议"
5. **防御性编程**：Try-Catch + 正则去噪 + Fallback textarea 兜底，绝不白屏
6. 部署到 **GitHub Pages**，无后端依赖，仅供小范围使用

## 下一版本 (v2) — 新增口语评分

### 核心新增能力

1. **首页模块选择**：Writing / Speaking 双入口卡片，用户选择后进入对应工作区
2. **录音 + 转写链路**：MediaRecorder API 录制 → Groq Whisper（免费 STT）→ 转写文本
3. **音频元数据分析**：前端 Web Audio API 提取语速、停顿、音量变化等量化指标
4. **口语 4 维度评分**：FC（流利度与连贯性）/ LR（词汇）/ GRA（语法）/ P（发音，间接推断）
5. **流利度可视化**：Canvas/SVG 绘制 WPM 仪表盘、停顿分布图等（口语独有 Tab）
6. **双 API 管理**：DeepSeek（评分）+ Groq（STT），两个 Key 在同一设置弹窗管理

### 技术方案

| 环节 | 技术 | 费用 |
|------|------|------|
| 音频录制 | MediaRecorder API (WebM Opus) | 免费（浏览器内置） |
| 音频分析 | Web Audio API (AudioContext) | 免费（浏览器内置） |
| 语音转文字 | Groq Whisper (`whisper-large-v3`) | **无限免费** |
| 口语评分 | DeepSeek Chat API | 新用户 500 万 Token 免费 |
| 流利度图表 | Canvas / SVG | 零依赖 |

### 口语数据流

```
用户录音 → 前端音频分析 (WPM/停顿/音量)
         → Groq Whisper (STT 转写)
         → 前端文本分析 (填充词/自我纠正/TTR)
         → DeepSeek (口语评分)
         → UI 渲染 (6 Tab + 流利度图表)
```

## 下一版本 (v3) — 新增听力同义替换

### 核心新增能力

1. **首页三模块选择**：Writing / Speaking / Listening 三张入口卡片
2. **大纲展示**：8 大场景分类、115 组同义词对、搜索过滤、陷阱词参考卡片
3. **游戏化练习**：随机 10 轮同义词匹配，target 词 + tile grid 点选，正确/误选/漏选即时着色
4. **成绩追踪**：正确率、连对、分类掌握度、错误回顾、重练错题
5. **完全零 API**：纯客户端运行，无需任何 API Key，即开即玩

### 技术方案

| 环节 | 技术 | 费用 |
|------|------|------|
| 同义词数据 | JS 常量嵌入 (LISTENING_CATEGORIES) | 免费（零请求） |
| 游戏引擎 | 纯 JavaScript DOM 操作 | 免费（浏览器内置） |
| 结果导出 | Blob + URL.createObjectURL | 免费（浏览器内置） |

### 听力数据流

```
用户选择分类范围 → 随机抽取 10 组同义词对
  → 随机选 target 词 (题干或音频侧)
  → 生成 tile grid (正确答案 + 干扰项，含中文释义)
  → 用户点选所有同义词 → 提交判定
  → 正确(绿) / 误选(红) / 漏选(灰) 着色反馈
  → 侧边栏实时更新进度 / 正确率 / 连对 / 分类掌握度
  → 10 轮结束后展示完整结果 + 错误回顾 → 可选导出 Markdown
```

## 文档索引

项目详细设计文档位于 `DOCS/` 目录下：

| 编号 | 文档 | 内容 |
|------|------|------|
| 01 | [评分体系与量化对齐设计](DOCS/评分体系与量化对齐设计.md) | 写作 4 维度 + 口语 4 维度评分标准、Task 差异、前后端量化约束 |
| 02 | [UI 与交互设计](DOCS/UI与交互设计.md) | 首页三入口、写作工作区、口语工作区、听力工作区、响应式 |
| 03 | [技术架构](DOCS/技术架构.md) | 写作链路、口语链路、听力链路、技术选型、API 配置、模块结构 |
| 04 | [核心 Prompt Engineering 设计](DOCS/Prompt工程与设计.md) | 写作 System/User Prompt、口语 System/User Prompt、JSON Schema |
| 05 | [详细开发与构建步骤](DOCS/开发与构建步骤.md) | 写作 + 口语 + 听力开发步骤 |
| 06 | [GitHub Pages 部署](DOCS/部署说明.md) | 部署方式、安全注意事项、使用流程 |
| 07 | [已知局限与风险](DOCS/已知局限与风险.md) | 写作 + 口语 + 听力风险及应对 |
| 08 | [未来可能的优化方向](DOCS/未来优化方向.md) | 写作 + 口语 + 听力未来规划 |
| 09 | [口语功能整体设计](DOCS/口语功能整体设计.md) | 口语功能概述、录音方案、STT、音频分析、评分、结果展示、API 汇总 |
| 10 | [听力同义替换设计](DOCS/听力同义替换设计.md) | 听力功能概述、数据结构、游戏机制、大纲展示、计分逻辑 🆕 |
